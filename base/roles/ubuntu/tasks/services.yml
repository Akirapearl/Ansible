---
  - name: Get photoprism config file for docker-compose & Place it locally
    ansible.builtin.shell: cd /home/melo/drive/Apps/Photoprism && wget https://dl.photoprism.org/docker/docker-compose.yml
